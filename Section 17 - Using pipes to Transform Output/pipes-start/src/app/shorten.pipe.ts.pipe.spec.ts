import { Shorten.Pipe.TsPipe } from './shorten.pipe.ts.pipe';

describe('Shorten.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new Shorten.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
